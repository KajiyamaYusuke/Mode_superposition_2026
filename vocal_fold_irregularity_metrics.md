# 左右声帯振動の不規則性を判定する指標

## 1. 目的

本解析では，左右声帯の剛性差を増加させたときに，

- 左右声帯が同一周期で同期しているか
- 同期したまま振幅・位相だけが変動しているか
- 高次の周波数ロッキングが生じているか
- 位相滑りや準周期振動が生じているか
- 非周期的・不規則な振動へ移行しているか

を定量的に判定することを目的とする。

現行モデルでは，流体力による励起の影響から第1固有モードの寄与が大きいため，
左右の構造的な離調については，まず第1固有振動数比

\[
r_f = \frac{f_{1,R}}{f_{1,L}}
\]

を代表指標として用いる。

ただし，以下に示す不規則性判定は固有振動数そのものではなく，
**実際の時間応答**である左右声帯変位，声門面積，およびそれらから抽出した周期・位相・ピーク列を用いて行う。

---

# 2. 使用する基本時系列

最低限，以下の時系列を保存する。

- `time`
- `x_left` : 左声帯中央部の内外側方向変位
- `x_right` : 右声帯中央部の内外側方向変位
- `glottal_area` : 声門最小面積または代表声門面積
- `flow_rate` : 声門流量（任意だが推奨）

解析対象区間は，圧力立ち上げ直後の過渡応答を除き，
振動が十分発達した定常または準定常区間とする。

---

# 3. 左右それぞれの周期列

## 3.1 計測方法

左右変位 `x_left(t)`, `x_right(t)` から局所極大値を検出する。

左声帯ピーク時刻を

\[
t_{L,1}, t_{L,2}, \dots
\]

右声帯ピーク時刻を

\[
t_{R,1}, t_{R,2}, \dots
\]

とする。

隣接ピーク間隔から周期列を求める。

\[
T_{L,n}=t_{L,n+1}-t_{L,n}
\]

\[
T_{R,n}=t_{R,n+1}-t_{R,n}
\]

必要に応じて，ピーク検出には `prominence` や最小ピーク間隔を設定し，
微小な数値ノイズをピークとして誤検出しないようにする。

## 3.2 何を見る指標か

この指標は，

**左右声帯が実際に同じ振動周期で動いているか**

を見る。

剛性差が存在していても，流体力や接触によって1:1同期していれば，

\[
T_L \approx T_R
\]

となる。

一方，左右の周期が異なる場合には，

- 1:1同期の崩壊
- 2:1，3:2などの高次ロッキング
- 準周期振動
- 非同期振動

の可能性がある。

## 3.3 推奨する出力値

- `mean_period_left`
- `mean_period_right`
- `std_period_left`
- `std_period_right`
- `cv_period_left`
- `cv_period_right`

ここで周期変動係数は

\[
CV_T=\frac{\sigma_T}{\bar T}
\]

とする。

---

# 4. 左右ピーク数比

## 4.1 計測方法

一定の解析時間窓 \(\Delta t\) の中で，
左右声帯の変位ピーク数を数える。

\[
N_L = \text{左声帯ピーク数}
\]

\[
N_R = \text{右声帯ピーク数}
\]

ピーク数比

\[
N_R:N_L
\]

を求める。

## 4.2 何を見る指標か

この指標は，

**左右の振動回数が整数比でロックしているか**

を見る。

例：

- `1:1` : 左右とも同じ回数振動
- `2:2` : 周期倍化しているが左右は1:1対応
- `1:2` : 一方が他方の2倍の周期
- `3:2`
- `5:8`

など。

左右の平均周波数だけを見るより，
高次の周波数ロッキングを直接判定しやすい。

## 4.3 注意

単純に解析区間全体のピーク数だけを比べると，
位相滑りが1回だけ起きた場合などを見逃す可能性がある。

そのため，必要に応じて解析窓を複数に分割し，
局所的なピーク数比も評価する。

---

# 5. 左右位相差

## 5.1 計測方法A：ピーク時刻差

各周期について，対応する左右ピークの時間差

\[
\Delta t_n = t_{R,n}-t_{L,n}
\]

を求める。

基準周期 \(T_n\) を用いて位相差へ変換する。

\[
\Delta \phi_n
=
2\pi \frac{\Delta t_n}{T_n}
\]

または角度表示で

\[
\Delta \phi_n
=
360^\circ \frac{\Delta t_n}{T_n}
\]

とする。

## 5.2 計測方法B：Hilbert変換

左右変位にHilbert変換を適用し，瞬時位相

\[
\phi_L(t), \phi_R(t)
\]

を求め，

\[
\Delta\phi(t)
=
\phi_R(t)-\phi_L(t)
\]

を計算する。

必要に応じて位相アンラップを行う。

## 5.3 何を見る指標か

この指標は，

**左右声帯の同期が保たれているか**

を最も直接的に見る。

### 1:1同期

\[
\Delta\phi(t)\approx \text{一定}
\]

であれば，左右で振幅が異なっていても1:1同期している。

### 同期したまま位相変動

\[
\Delta\phi(t)
\]

が一定値の周囲で揺らぐ場合は，
1:1同期を保ちながら位相差が変動している状態と解釈できる。

### 位相滑り・非同期

アンラップした \(\Delta\phi(t)\) が時間とともに増加・減少し続ける場合，

\[
\Delta\phi(t)\sim \omega_{\rm slip} t
\]

となり，左右同期が外れている可能性が高い。

## 5.4 推奨する出力値

- `mean_phase_diff`
- `std_phase_diff`
- `phase_diff_range`
- `phase_drift_rate`

位相ドリフト率は，アンラップ位相差に対して線形近似を行い，

\[
k_\phi = \frac{d\Delta\phi}{dt}
\]

として求める。

`phase_drift_rate ≈ 0` なら同期，
非ゼロ値が持続するなら位相滑りと判断できる。

---

# 6. 左右振幅列

## 6.1 計測方法

左右変位の各局所極大値を

\[
A_{L,1},A_{L,2},\dots
\]

\[
A_{R,1},A_{R,2},\dots
\]

として保存する。

平均値，標準偏差，変動係数を求める。

\[
CV_A=\frac{\sigma_A}{\bar A}
\]

## 6.2 何を見る指標か

この指標は，

**周期ごとの振幅が一定か，不規則に変動しているか**

を見る。

例えば，

- ほぼ一定値 → 規則的周期振動
- 大小が交互 → 周期倍化
- 3値程度を繰り返す → period-3
- 規則的な包絡変動 → 準周期の可能性
- 明確な繰り返しなし → 非周期的振動の可能性

となる。

## 6.3 注意

振幅変動だけでは，

- 周期倍化
- 準周期
- カオス的変動

を一意に区別できない。

必ず周期列・位相差・相軌道・スペクトルと併用する。

---

# 7. 声門面積の極大値列

## 7.1 計測方法

`glottal_area(t)` から局所極大値を検出し，

\[
A_{g,1}^{\max},A_{g,2}^{\max},\dots
\]

を求める。

各ピーク間隔

\[
T_{g,n}
=
t_{g,n+1}^{\max}-t_{g,n}^{\max}
\]

も保存する。

## 7.2 何を見る指標か

これはZhang型の評価に対応し，

**声帯全体としての開閉運動が周期的か，不規則か**

を見る。

左右声帯変位がそれぞれ規則的でも，
左右の位相関係や接触が変化すると，
声門面積波形は不規則になる場合がある。

したがって，

- 左右個別の不規則性
- 系全体としての声門開閉の不規則性

を分けて評価できる。

## 7.3 推奨する出力値

- `mean_glottal_peak`
- `std_glottal_peak`
- `cv_glottal_peak`
- `mean_glottal_period`
- `std_glottal_period`
- `cv_glottal_period`

---

# 8. 相軌道

## 8.1 計測方法

横軸を左声帯変位，
縦軸を右声帯変位として，

\[
(x_L(t),x_R(t))
\]

をプロットする。

## 8.2 何を見る指標か

これは，

**左右声帯の結合運動が同じ軌道を繰り返しているか**

を見る。

代表的な解釈：

- 一本の閉曲線  
  → 1:1周期同期

- 二重に折り畳まれた閉曲線  
  → 周期倍化

- 複数ループ  
  → 高次周期・高次ロッキング

- 帯状の軌道  
  → 準周期振動の可能性

- 閉曲線へ収束しない複雑な集合  
  → 非周期振動の可能性

## 8.3 注意

相軌道は視覚的評価として非常に有効だが，
自動判定にはそのまま使いにくい。

Codexで自動化する場合は，
画像生成用データとして保存し，
状態分類そのものは周期列・位相差・ピーク列で行う方がよい。

---

# 9. 周波数スペクトル

## 9.1 計測方法

左右変位および声門面積に対してFFTを行う。

対象：

- `x_left`
- `x_right`
- `glottal_area`

必要に応じて平均値を除去し，
Hann窓などを使用する。

## 9.2 何を見る指標か

スペクトルでは，

**複数周期成分や独立周波数が存在するか**

を見る。

### 1:1周期振動

基本周波数 \(f_0\) とその倍音が主に現れる。

### 周期倍化

元の基本周期に対して

\[
f_0/2
\]

のサブハーモニクスが現れる。

### period-3

\[
f_0/3
\]

に対応する成分が現れる場合がある。

### 準周期

二つの独立周波数

\[
f_1,\ f_2
\]

と，

\[
m f_1+n f_2
\]

の組合せ成分が現れる。

### 非周期振動

離散ピーク構造が崩れ，
広帯域成分が増加する場合がある。

## 9.3 注意

スペクトルだけで「カオス」と断定しない。

サブハーモニクスの存在は，
周期倍化や高次周期振動でも生じる。

---

# 10. 位相滑り回数

## 10.1 計測方法

アンラップした位相差 \(\Delta\phi(t)\) を用い，

\[
2\pi
\]

を超える位相差変化が何回生じたかを数える。

例えば，

\[
\Delta\phi(t)
\]

が解析区間中に \(2\pi\) 増加すれば，
一度の位相滑りが生じたとみなす。

## 10.2 何を見る指標か

これは，

**1:1同期が一時的または持続的に破れているか**

を見る。

位相差の標準偏差が大きいだけでは，
同期したまま大きく揺れている状態と，
本当に同期が外れた状態を区別しにくい。

位相滑り回数を併用することで，
同期崩壊を明確に判定しやすくなる。

## 10.3 推奨出力値

- `phase_slip_count`
- `phase_slip_rate`

\[
\text{phase slip rate}
=
\frac{\text{phase slip count}}{\text{analysis duration}}
\]

---

# 11. 周期・振幅の反復性

## 11.1 計測方法

ピーク振幅列

\[
A_n
\]

または周期列

\[
T_n
\]

について，
1周期後，2周期後，…と比較する。

例えば遅れ \(k\) に対して，

\[
D_A(k)
=
\frac{1}{N-k}
\sum_{n=1}^{N-k}
|A_{n+k}-A_n|
\]

を求める。

周期列についても同様に

\[
D_T(k)
=
\frac{1}{N-k}
\sum_{n=1}^{N-k}
|T_{n+k}-T_n|
\]

を求める。

## 11.2 何を見る指標か

これは，

**複数周期で同じパターンが繰り返されているか**

を見る。

例えばperiod-2なら，

\[
D_A(2)
\]

は小さく，

\[
D_A(1)
\]

は大きくなる。

period-3なら

\[
D_A(3)
\]

が小さくなる。

どの \(k\) に対しても小さくならなければ，
非周期的な振動の可能性が高い。

## 11.3 推奨範囲

\[
k=1,\dots,10
\]

程度。

これはZhangで行われた「1～10周期後の反復性を見る」という考え方に対応する。

---

# 12. 自己相関

## 12.1 計測方法

左右変位または声門面積に対して自己相関関数

\[
R(\tau)
\]

を計算する。

## 12.2 何を見る指標か

規則的周期振動では，
基本周期およびその整数倍の遅れで
自己相関が高くなる。

準周期や非周期になると，
ピークが低下したり，
長時間で反復性が失われる。

## 12.3 位置づけ

主判定指標ではなく，
ピーク列やスペクトルによる判定の補助として用いる。

---

# 13. 摂動後の同期回復率

## 13.1 計測方法

安定した1:1同期状態に対して，
片側声帯の変位またはモード座標に小さな摂動を与える。

基準同期解との差を

\[
\delta x_L,\ \delta x_R
\]

とし，
左右差方向の摂動を

\[
\delta x_a
=
\frac{\delta x_L-\delta x_R}{2}
\]

とする。

一周期 \(T\) ごとに，

\[
r_n
=
\frac{|\delta x_a(t+(n+1)T)|}
{|\delta x_a(t+nT)|}
\]

を求める。

## 13.2 何を見る指標か

この指標は，

**現在の1:1同期状態がどの程度安定か**

を見る。

- \(r<1\)  
  → 摂動が減衰し，同期状態へ戻る

- \(r\approx1\)  
  → 中立的・境界付近

- \(r>1\)  
  → 非対称摂動が成長し，同期解が不安定

不規則振動がまだ現れていない条件でも，
同期境界にどの程度近いかを評価できる。

---

# 14. 推奨する状態分類

最初の自動分類は，以下の4段階程度でよい。

## Type A：安定1:1同期

条件例：

- 左右平均周期がほぼ一致
- ピーク数比が1:1
- 位相差がほぼ一定
- 位相滑りなし
- 振幅列・周期列の変動が小さい

解釈：

**左右剛性差があっても流体・接触結合によって安定に同期している。**

---

## Type B：1:1同期内の変調

条件例：

- 左右平均周期は一致
- ピーク数比は1:1
- 位相滑りなし
- 位相差または振幅が周期ごとに変動
- 声門面積ピークも変動

解釈：

**基本的な周波数同期は維持しているが，振幅・位相の変調が生じている。**

現行モデルの強い非対称条件で一部観察されている状態は，
まずこの分類に入る可能性が高い。

---

## Type C：高次ロッキング・複数周期

条件例：

- 左右ピーク数比が1:1以外，または
- period-2，period-3など明瞭な反復構造
- スペクトルにサブハーモニクス
- 相軌道が複数ループ

解釈：

**単純な1:1周期同期から別の周期解へ分岐した状態。**

---

## Type D：非同期・非周期

条件例：

- 左右平均周期が異なる
- 位相差が持続的にドリフト
- 位相滑りが反復
- 1～10周期後の反復性が低い
- 相軌道が閉曲線に収束しない
- スペクトルが複雑または広帯域化

解釈：

**左右の1:1同期が失われ，準周期または非周期振動へ移行している。**

---

# 15. 推奨する自動解析出力

各計算条件について，1行のCSVとして以下を出力する。

```text
condition_id
body_left
cover_left
body_right
cover_right
f1_left
f1_right
f1_ratio
subglottal_pressure

mean_period_left
mean_period_right
std_period_left
std_period_right
cv_period_left
cv_period_right

peak_count_left
peak_count_right
peak_count_ratio

mean_phase_diff
std_phase_diff
phase_diff_range
phase_drift_rate
phase_slip_count
phase_slip_rate

mean_amp_left
mean_amp_right
std_amp_left
std_amp_right
cv_amp_left
cv_amp_right

mean_glottal_peak
std_glottal_peak
cv_glottal_peak
mean_glottal_period
std_glottal_period
cv_glottal_period

dominant_freq_left
dominant_freq_right
subharmonic_half_power
subharmonic_third_power

repeat_error_amp_k1
repeat_error_amp_k2
...
repeat_error_amp_k10

repeat_error_period_k1
...
repeat_error_period_k10

state_type
```

---

# 16. Codex実装時の優先順位

最初からすべてを実装する必要はない。

まず以下の5項目を優先する。

1. 左右周期列
2. 左右位相差
3. 位相滑り回数
4. 左右ピーク数比
5. 声門面積ピーク列

この5つだけでも，

- 1:1同期
- 同期内変調
- 高次ロッキング
- 非同期

をかなり区別できる。

その後，

6. 相軌道
7. FFT
8. 1～10周期後の反復性
9. 自己相関
10. 摂動後の同期回復率

を追加する。

---

# 17. 現行研究で特に重要な判定

今回最も知りたいのは，

**「左右の第1固有振動数比をどこまで低下させると，1:1同期が破れるか」**

である。

したがって，最終的には

\[
r_f=\frac{f_{1,R}}{f_{1,L}}
\]

と声門下圧 \(P_s\) をパラメータとして，

- Type A
- Type B
- Type C
- Type D

の状態図を作成する。

特に重要なのは，

**位相差が大きいかどうかではなく，位相差が時間的に固定されているか，ドリフトするか**

を区別することである。

位相差が大きくても一定なら1:1同期であり，
位相差が時間とともに流れ続ける場合に初めて，
左右の周波数同期が崩れていると判断する。
