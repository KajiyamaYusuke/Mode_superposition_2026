# 接触・流体作用の残修正項目

更新日: 2026-07-22

## 1. この文書の目的

今後調べたい主因は、次の3点である。

- 使用モード数・参加モードの影響
- 左右の固有周波数差の影響
- 声門下圧の影響

これらを正しく比較するには、接触モデルと流体モデルが解析パラメータの変更によって意図せず変化せず、同じ形状・同じ時刻に対して再現性のある力を返す必要がある。

本書では、現行コードのうち接触作用と流体作用について、

1. すでに改善できている点
2. パラメータ解析前に必ず直す点
3. 次段階で改善する点
4. 検証試験と合格条件

を整理する。

---

# 2. 現行コードで改善できている点

## 2.1 流体断面

現行の `ChannelSectionBuilder` では、旧来の表面格子番号 `i` をそのまま流体断面とせず、変形後の共通物理平面

\[
x=x_k
\]

と左右表面との交点を補間して断面を作っている。

また、断面積は左右表面を単純な多角形として絶対値評価する方式から、局所ギャップ

\[
g(z)=y_R(z)-y_L(z)
\]

の正の部分だけを積分する方式へ変更されている。

\[
A(x)=\int \max(g(z),0)\,dz
\]

これにより、数値的に左右表面が交差したときに、閉鎖断面が偽の正面積として再出現する問題は改善されている。

関連箇所:

- `channelSections.cpp`
- `ChannelSectionBuilder::build()`
- `positiveGapAreaYZ()`
- `intersectAtX()`

## 2.2 表面圧力マッピング

表面圧力は、左右の同じ `i` 番号へ同じ値を与える方式ではなく、各表面点の変形後座標

\[
x_{\mathrm{node}}
\]

から共通流路断面の圧力を補間する方式になっている。

これにより、左右の表面が流れ方向へ異なる変形をしても、同じ物理位置を基準に圧力を与えられる。

関連箇所:

- `forceCalculator.cpp`
- `ForceCalculator::applyFluidLoads()`
- `pressureAtSurfaceX()`

## 2.3 流体力方向

流体力は、表面傾斜角を独立に組み合わせる旧方式ではなく、変形後表面の接線ベクトルの外積から面積ベクトルを作る方式へ改善されている。

\[
d\boldsymbol A
=
\boldsymbol t_i\times\boldsymbol t_j
\]

\[
d\boldsymbol F=p\,d\boldsymbol A
\]

この改変により、流れ方向・開閉方向・長手方向へ同時に傾いた面でも、圧力方向を一貫して計算できる。

## 2.4 剥離位置

剥離位置は、材料座標や表面格子番号ではなく、共通流路断面の実座標

\[
x_{\mathrm{sep}}
\]

を基準に表面荷重へ反映する構造になっている。

`separation.dat` には、

- 剥離断面番号
- `x_sep`
- `x_blend_end`
- 剥離圧力

が出力される。

## 2.5 接触探索

接触候補は、左右の同じ `i` 番号を直接比較するのではなく、各 `j` 線上で左右の変形後 \(x\) 線分が重なる区間を探索し、その区間内の負ギャップを積分している。

接触力は左右へ等大・反対向きに分配され、接近時だけ速度減衰を加える構造になっている。

---

# 3. 最優先で修正する項目

以下は、モード数・固有周波数・圧力のパラメータ解析を始める前に修正すべき項目である。

---

## P0-1. 接触反復の最終力と確定変位を一致させる

### 現状

時間ステップ内の接触反復は概ね次の順序である。

1. 現在の表面荷重をモード力へ射影
2. Newmark法で構造応答を予測
3. モード変位から節点変位を復元
4. 予測変位から新しい接触力を計算
5. 表面荷重を更新
6. 接触なし、または力差が小さければ終了

この順序では、手順4で新しく計算した接触力は、次の反復で初めて構造応答へ反映される。

したがって、次の問題が起こり得る。

- `ncont=1` では、新しい接触力がその時間ステップの変位へ反映されない
- 接触が消えた反復で終了すると、除去前の接触力で計算した変位が確定される
- 最大反復回数へ達すると、最後に更新した接触力と確定変位が一致しない
- 接触係数の影響と反復誤差を分離できない

### 必要な修正

接触力を反復変数として明示的に管理する。

```text
contact_old = 初期接触力

反復:
    total_load = fluid_load + contact_old
    committed state から構造を解く
    predicted displacement を作る
    contact_new を predicted displacement から計算
    residual = contact_new - contact_old
    contact_old = relaxation(contact_old, contact_new)

収束後:
    収束した contact_old を使って構造をもう一度解く
    その変位だけを確定する
```

### 収束判定

絶対力差だけでなく、正規化残差を使う。

\[
r_F=
\frac{\lVert F_c^{k+1}-F_c^k\rVert}
{\max(\lVert F_c^{k+1}\rVert,F_{\mathrm{ref}})}
\]

さらに、最大めり込み変化も確認する。

\[
r_\delta=
|\delta_{\max}^{k+1}-\delta_{\max}^{k}|
\]

### 合格条件

推奨する初期基準:

- 最終接触力で再計算した変位を確定している
- `ncont` を増やしても最大めり込み、接触時間、振幅がほぼ変わらない
- 接触反復数の上限到達が常態化していない
- 接触が消えた場合にも、接触力ゼロの状態で構造を再計算している

---

## P0-2. 接触係数を固有周波数から切り離す

### 現状

接触圧は、左右1次固有角振動数の二乗平均

\[
\omega_{\mathrm{ref}}^2
=
\frac{\omega_{1,L}^2+\omega_{1,R}^2}{2}
\]

を使い、概ね次式で計算されている。

\[
p_c
=
k_{c1}\omega_{\mathrm{ref}}^2\delta
\left(1+k_{c2}\omega_{\mathrm{ref}}^2\delta^2\right)
+
k_{c3}\dot\delta_+
\]

この実装では、固有周波数を変更すると、構造特性だけでなく接触剛性も同時に変化する。

したがって、固有周波数スイープの結果に、

- 構造固有周波数の効果
- 接触剛性が変化した効果

が混在する。

### 必要な修正

接触則を物理単位を持つ係数で定義する。

候補:

\[
p_c=k_1\delta+k_3\delta^3+c_c\dot\delta_+
\]

単位:

- \(k_1\): Pa/m
- \(k_3\): Pa/m³
- \(c_c\): Pa·s/m

またはHunt–Crossley型:

\[
p_c=k\delta^n(1+\alpha\dot\delta)
\]

### 暫定対応

接触則をすぐ変更できない場合は、周波数スイープ中だけ

\[
\omega_{\mathrm{ref}}
\]

を全ケース共通の固定値にする。

ただし、最終的には物理単位付き接触則へ変更する。

### 合格条件

- 固有周波数を変更しても、同じ \(\delta,\dot\delta\) に対する接触圧が変わらない
- 接触係数の意味を、めり込み剛性・非線形硬化・減衰として独立に説明できる

---

## P0-3. 接触のギャップ方向と反力方向を一致させる

### 現状

接触判定には主に

\[
g_y=y_R-y_L
\]

を使っている。

一方、接触力は左右表面の \(xy\) 接線から求めた法線

\[
\boldsymbol n=(n_x,n_y)
\]

方向へ与えている。

そのため、面が傾いている場合、

- めり込みを測る方向
- 反力を与える方向

が一致しない。

### 必要な修正

法線ギャップを用いる。

\[
g_n=
(\boldsymbol r_R-\boldsymbol r_L)\cdot\boldsymbol n
\]

\[
\delta=\max(-g_n,0)
\]

速度項も同じ法線で計算する。

\[
\dot\delta_+
=
\max\left(
-(\boldsymbol v_R-\boldsymbol v_L)\cdot\boldsymbol n,
0
\right)
\]

### 合格条件

- 平行面では従来結果と一致する
- 傾斜面でも接触力が面法線方向に作用する
- 接触力が負、すなわち引力にならない
- 接触がない状態で接触エネルギーが増加しない

---

## P0-4. 面積計算と接触計算で共通の物理 \(z\) を使う

### 現状

断面積も接触も、基本的には左右の同じ `j` を対応させている。

しかし、長手方向変位がある場合、

\[
z_L(j)\neq z_R(j)
\]

となる可能性がある。

また、断面積積分では左側の \(z\) 差を積分幅として使っている。

これは低次モードで \(z\) 変位が小さい場合には近似として使えるが、高次モードや長手方向に非一様な振動を調べる際には誤差源になる。

### 必要な修正

各流路断面で左右曲線を共通の物理 \(z\) グリッドへ補間する。

```text
左曲線: yL(z)
右曲線: yR(z)
共通z点: z0, z1, ...
g(z) = yR(z) - yL(z)
```

面積:

\[
A(x)=\int\max(g(z),0)\,dz
\]

接触も同じ \(z\) グリッドと同じギャップ定義を使用する。

### 合格条件

- 面積がゼロに近い断面と接触領域が一致する
- \(z\) 変位をゼロにした場合は現行結果とほぼ一致する
- 長手方向モードを追加しても `j` 対応の破綻が起きない

---

## P0-5. 出力時刻を統一する

### 現状

現在のループでは、

- `area.dat`
- `section_x.dat`
- `pressure.dat`

は時間ステップ冒頭の確定変位から計算される。

一方、変位出力は接触反復後の予測・更新変位を使う箇所がある。

同じ `step` や `time` が付いていても、

- 圧力・面積は \(t_n\)
- 変位は \(t_{n+1}\)

となる可能性がある。

これは圧力と変位の位相差、流体仕事、周波数応答の解析を誤らせる。

### 必要な修正

出力を次のどちらかへ統一する。

#### 方針A: 更新前出力

すべて \(t_n\) の確定状態を出力してから時間積分する。

#### 方針B: 更新後出力

構造・接触・流体の更新後に、すべて \(t_{n+1}\) として出力する。

推奨は方針Bである。

### 合格条件

- 同一行の面積、圧力、流量、変位、接触力が同じ物理時刻を表す
- 出力ヘッダに `step` と `time_s` を明記する
- 圧力―変位位相を人工的に1ステップずらさず評価できる

---

## P0-6. 流体デバッグ出力を圧力計算後へ移動する

### 現状

`debug_flow_detail.csv` の出力処理が、現時刻の `psurf` を再計算する前に実行される構造になっている。

その場合、ログ中の `psurf` は、

- 前ステップの値
- 初期値
- 現在の流量・面積と対応しない値

になる可能性がある。

### 必要な修正

順序を次のように統一する。

1. 断面積更新
2. `FlowModel::advance()`
3. `psurf` 更新
4. 表面圧力補間
5. 流体力計算
6. デバッグ出力

### 合格条件

- `debug_flow_detail.csv` の `currentUg`, `currentPg`, `psurf` が同一時刻
- `pressure.dat` とデバッグCSVの該当値が一致する

---

## P0-7. 流体状態を `FlowModel` へ一本化する

### 現状

`FlowModel` が、

- `Uu_`
- `Pu_`
- `Ud_`
- `Pd_`

を保持している一方、`ForceCalculator` にも同名・類似の配列と音響回路係数が残っている。

実際の更新は `FlowModel` 側で行われているが、古い配列が残ると、

- デバッグ出力が未使用配列を参照する
- どちらが真の流体状態か分からなくなる
- 将来の修正で二重更新が生じる

危険がある。

### 必要な修正

- 流量・上流圧・下流圧・回路係数は `FlowModel` のみで保持する
- `ForceCalculator` は必要な値をgetterで参照する
- 古い重複変数と旧流体更新関数を削除する

### 合格条件

- 流体状態の所有者が一つ
- 同じ物理量を表す配列が複数存在しない
- ログは必ず `FlowModel` の状態から出力される

---

# 4. 流体モデルで優先して改善する項目

---

## P1-1. 重複する `section_x` を解消する

### 現状

流路断面位置は、すべての表面線で交差可能な共通範囲

\[
[ x_{\mathrm{commonLow}},x_{\mathrm{commonHigh}} ]
\]

へクランプされる。

そのため出口付近で複数断面が同じ座標に集まり、

\[
x_k=x_{k+1}
\]

となることがある。

### 影響

- 圧力積分の \(\Delta x\) がゼロ
- 隣接断面補間の意味が曖昧
- 剥離後遷移長がゼロ
- 最小面積断面番号が重複断面間で移動
- `FluidPlaneCoordinate` と物理距離が一対一対応しない

### 必要な修正

候補A:

共通物理範囲に毎時刻等間隔で断面を配置する。

\[
x_k=x_{\min}+\frac{k}{N-1}(x_{\max}-x_{\min})
\]

候補B:

現在の材料座標由来位置を維持しつつ、重複断面を削除して有効断面数を可変にする。

### 合格条件

- 有効断面について \(x_{k+1}>x_k\)
- ゼロ幅区間がない
- 断面数を50から100へ変更しても主要結果が大きく変わらない

---

## P1-2. 剥離後の圧力遷移を物理距離で指定する

### 現状

遷移終了位置は概ね

```text
blendLastIndex = sepIndex + 2
```

として選ばれている。

重複断面があると、

\[
x_{\mathrm{blendEnd}}-x_{\mathrm{sep}}=0
\]

になり、滑らかな遷移が実質的に消える。

### 必要な修正

\[
x_{\mathrm{blendEnd}}
=
\min(x_{\mathrm{sep}}+L_{\mathrm{blend}},x_{\mathrm{out}})
\]

として物理距離で指定する。

`L_blend` はパラメータファイルから変更可能にする。

### 合格条件

- `x_blend_end > x_sep` が確保される
- 遷移幅が断面数に依存しない
- 圧力の段差がない
- `L_blend` を変えた感度解析が可能

---

## P1-3. 流量計算に声門全体の断面分布を使う

### 現状

声門流量計算の主要な抵抗・慣性は最小断面積

\[
A_{\min}
\]

だけから計算される。

概略:

\[
L_g\propto\frac{1}{A_{\min}}
\]

\[
R_k\propto\frac{1}{A_{\min}^2}
\]

\[
R_v\propto\frac{1}{A_{\min}^3}
\]

一方、壁面圧力分布には50断面の面積列を使っている。

そのため、

- 流量は最小面積だけで決まる
- 壁面圧力は断面形状分布で決まる

という不整合がある。

モード形状を変えたとき、最小面積が同じでも狭窄長や上下形状が異なるケースを区別できない。

### 必要な修正

少なくとも声門慣性を、

\[
L_g
=
\rho\int\frac{dx}{A(x)}
\]

に基づいて計算する。

粘性抵抗には、次項の \(G_3(x)\) を使う。

局所損失 \(R_k\) は最小断面積に依存する項として残してよいが、分布抵抗と区別する。

### 合格条件

- 同じ \(A_{\min}\) で狭窄長が異なる形状に対して流量が異なる
- 断面数を変えても積分抵抗・慣性が収束する
- 単純な矩形流路で解析式と一致する

---

## P1-4. \(\int g^3dz\) を保存し、粘性抵抗に使う

### 現状

圧力計算では断面積 \(A\) から平均ギャップ

\[
h=A/l_g
\]

を作り、粘性損失を計算している。

しかし、長手方向にギャップが不均一な場合、

\[
\left(\frac{A}{l_g}\right)^3l_g
\neq
\int g(z)^3dz
\]

である。

部分閉鎖を伴う非対称振動では、この差が大きくなる。

### 必要な修正

各断面について、

\[
G_1(x)=\int g_+(z)\,dz=A(x)
\]

\[
G_3(x)=\int g_+(z)^3\,dz
\]

を計算して保存する。

ここで、

\[
g_+(z)=\max(g(z),0)
\]

粘性圧力損失は概ね、

\[
\frac{dp}{dx}
=
-\frac{12\mu U}{G_3(x)}
\]

を用いる。

### 合格条件

- 一様ギャップでは現行式と一致する
- 部分閉鎖・非一様ギャップでは局所形状差を反映する
- \(G_3\) がゼロへ近づくときの閉鎖処理が安定

---

## P1-5. 物理長を座標最大値ではなく座標範囲で定義する

### 現状

流体モデル内で、

- `geometry.xsup`
- `geometry.zmax`

に相当する値を長さとして使用している。

座標原点がゼロでない場合、最大座標は物理長ではない。

### 必要な修正

\[
L_x=x_{\max}-x_{\min}
\]

\[
L_z=z_{\max}-z_{\min}
\]

を使用する。

声門流路範囲については、

\[
L_{\mathrm{flow}}=
x_{\mathrm{lastSection}}-x_{\mathrm{firstSection}}
\]

を用いる。

### 合格条件

- モデル全体を座標移動しても計算結果が変わらない
- 左右モデルの座標原点が違っても同じ物理長を使える

---

## P1-6. 圧力回復を強制停止する処理を監視する

### 現状

剥離前に計算圧力が上昇した場合、

```text
psurf[i] = psurf[i-1]
```

として圧力回復を抑える安全処理がある。

これは発散防止には有効だが、頻繁に作動すると、計算式ではなくクランプ処理が圧力分布を決める。

### 必要な修正

- クランプ発生回数
- 発生断面
- 発生時刻
- クランプ前後の圧力差

を出力する。

### 合格条件

- 通常ケースでクランプが常態化していない
- クランプを外した場合と主要結果が大きく異ならない
- 圧力回復が物理的に必要な領域は、剥離モデルとして別に扱う

---

# 5. 接触モデルの追加改善

---

## P1-7. 接触係数を最大めり込みだけで調整しない

接触係数は次の指標を同時に使って調整する。

- 最大めり込み
- 接触継続時間
- 接触前後の相対法線速度
- 最大接触圧
- 接触力積分
- 接触による散逸エネルギー
- 1周期中の接触時間比

接触仕事:

\[
W_c=\int \boldsymbol F_c\cdot
(\boldsymbol v_R-\boldsymbol v_L)\,dt
\]

減衰項はエネルギーを除去し、人工的に加えるべきではない。

### 合格条件

- 接触減衰がエネルギーを生成しない
- `kc3` または \(c_c\) を増やしたとき、反発速度と振動が単調に減る
- めり込み低減のために減衰だけを過大化していない
- 接触が1周期の大部分を占めず、長時間拘束になっていない

---

## P1-8. 接触探索を3次元モードに対応させる

現行接触は、各 `j` ごとの \(xy\) 断面内で実行される。

これは実質的には「長手方向に並べた2次元接触」であり、完全な3次元面対面接触ではない。

長手方向モードの影響を調べるには、少なくとも共通物理 \(z\) 補間が必要である。

最終的な候補:

- 共通 \(z\) スライスによる接触
- 三角形面同士のsurface-to-surface接触
- 最近接点またはmortar法
- augmented Lagrangian法

当面は共通 \(z\) スライス方式でよい。

---

# 6. 流体・構造連成の時間遅れ

## P2-1. 流体力を接触反復中に固定したままにしない

### 現状

1時間ステップ中に、

1. 確定形状から面積を計算
2. 流量・圧力・流体力を計算
3. 流体力を固定
4. 接触反復で変位だけを修正

している。

接触によって声門面積が大きく変わっても、その時間ステップ内では流体圧が再計算されない。

### 影響

- 閉鎖時の圧力が1ステップ遅れる
- 圧力と接触の競合が時間刻みに依存する
- 声門下圧の効果と陽的連成誤差を分離しにくい

### 必要な修正

まずは1回の予測修正を追加する。

```text
確定形状から流体力A
→ 構造・接触を解く
→ 予測形状から面積と流体力Bを再計算
→ 流体力を緩和
→ 構造・接触をもう一度解く
```

完全な強連成へ移る前に、`dt` と予測修正回数の収束性を調べる。

### 合格条件

- `dt/2` で振幅、周波数、流量、最大めり込みが大きく変わらない
- 予測修正0回と1回の差が定量化されている
- 圧力―変位位相が時間刻みによって大きく移動しない

---

# 7. 力マッピングの検証

## P2-2. 流体力積分とモード一般化力を検証する

各時刻で次を保存する。

\[
\boldsymbol F_{\mathrm{fluid},L}
=
\sum_i\boldsymbol F_{i,L}
\]

\[
\boldsymbol F_{\mathrm{fluid},R}
=
\sum_i\boldsymbol F_{i,R}
\]

\[
M_{\mathrm{fluid}}
=
\sum_i
(\boldsymbol r_i-\boldsymbol r_0)
\times\boldsymbol F_i
\]

また各モードについて、

\[
f_m=
\sum_i\boldsymbol F_i\cdot\boldsymbol\phi_{m,i}
\]

を出力する。

### 合格条件

対称モデルでは、

- 左右の開閉方向合力が等大・反対向き
- 流れ方向合力が左右でほぼ一致
- 長手方向合力が対称性に応じて相殺
- 左右対応モードの一般化力が一致

非対称モデルでは、差が表面形状・モード形状から説明できる。

---

# 8. パラメータ解析前の検証試験

## 8.1 接触単体試験

### 試験C1: 平行面の静的めり込み

既知の一定めり込み \(\delta\) を与え、接触圧が接触則と一致するか確認する。

### 試験C2: 一定速度衝突

既知の接近速度を与え、

- 接触前速度
- 最大めり込み
- 接触後速度
- 散逸エネルギー

を確認する。

### 試験C3: 左右入れ替え

左右モデルを交換しても、力の大きさと物理結果が鏡映関係になることを確認する。

### 試験C4: 接触反復収束

`ncont` を増加させ、

- 最大めり込み
- 最大接触力
- 接触時間
- 変位振幅

が収束するか確認する。

---

## 8.2 流体単体試験

### 試験F1: 一様矩形流路

全断面で同じ面積を与え、解析式と流量・粘性損失を比較する。

### 試験F2: 単調収束流路

面積を単調に減少させ、

- 圧力が下流へ不自然に振動しない
- 最小断面で剥離が選ばれる
- 断面数変更で結果が収束する

ことを確認する。

### 試験F3: 左右表面の材料格子だけをずらす

物理形状を同じに保ち、左右の `i` 格子配置だけを変える。

物理断面、圧力、流体力が変化しなければ、実座標マッピングが健全である。

### 試験F4: 座標原点移動

モデル全体へ一定の \(x,z\) オフセットを加えても結果が変わらないことを確認する。

### 試験F5: 断面数収束

流路断面数を、

- 25
- 50
- 100

で比較する。

---

## 8.3 連成切り分け試験

| ケース | 流体 | 接触 | 目的 |
|---|---:|---:|---|
| S | なし | なし | 純粋なモード・固有周波数応答 |
| F | あり | なし | 共通流体圧による同期・振幅変化 |
| C | なし | あり | 接触だけの拘束・減衰効果 |
| FC | あり | あり | 完全連成結果 |

各ケースで共通して保存する量:

- 左右変位
- 左右速度
- モード座標 \(q_m,\dot q_m\)
- モード一般化力 \(f_m\)
- 流量
- 各断面面積・圧力
- 左右流体合力
- 接触力
- 最大めり込み
- 接触時間
- エネルギー収支

---

# 9. パラメータ解析開始の合格条件

以下を満たしてから、モード数・固有周波数・声門下圧の本スイープへ進む。

## 必須条件

- [ ] 最終接触力と確定変位が同一反復状態に対応する
- [ ] 接触係数が固有周波数変更によって自動的に変化しない
- [ ] 接触判定方向と接触力方向が一致する
- [ ] 面積計算と接触計算の閉鎖位置が一致する
- [ ] `area`, `pressure`, `airflow`, `displacement`, `contact` の時刻が一致する
- [ ] `debug_flow_detail.csv` が現時刻の `psurf` を記録する
- [ ] 流体状態が `FlowModel` に一本化されている
- [ ] 有効流路断面の \(x\) 座標が厳密に単調増加
- [ ] 剥離後遷移幅が物理距離で正になる
- [ ] `dt/2` で主要結果が収束する
- [ ] `ncont` 増加で接触結果が収束する
- [ ] 対称モデルで左右流体力が対称になる

## 推奨する数値的目安

以下は暫定的な工学基準であり、最終的にはメッシュ・材料・実験条件に合わせて調整する。

- 時間刻み半減による主周波数差: 1–2%以下
- 時間刻み半減による振幅・最大流量差: 5%以下
- 接触反復数増加による最大めり込み差: 5%以下
- 対称ケースの左右合力差: 1%程度以下
- 接触中に引力が発生しない
- 接触減衰による正味エネルギー生成がない

---

# 10. 実装の推奨順序

1. **接触反復の最終力―変位整合性を修正**
2. **接触則を固有周波数から切り離す**
3. **出力時刻と流体デバッグ順序を統一**
4. **流体状態を `FlowModel` へ一本化**
5. **重複 `section_x` を解消**
6. **剥離blendを物理距離化**
7. **面積と接触を共通物理 \(z\) へ補間**
8. **\(G_3=\int g^3dz\) を追加**
9. **分布面積から声門慣性・粘性抵抗を計算**
10. **流体―構造の予測修正を追加**
11. **接触単体・流体単体・連成切り分け試験**
12. **モード数・固有周波数・圧力スイープ開始**

---

# 11. 現時点で後回しにできる拡張

以下は有用だが、上記の基礎検証前に追加する必要はない。

- 2D/3D CFD
- Coandă効果
- 左右で異なる壁面圧力
- 完全な三角形面surface-to-surface接触
- 非線形超弾性材料
- \(z\) 方向multi-slice流体モデル

現行の共通1次元流体モデルでも、接触・流体・時間積分が整合していれば、左右離調、周期倍化、準周期化などの発生可能性を調べられる。

まずは、接触と流体の数値実装がモード数・固有周波数・圧力の変更を汚染しない状態を作ることを優先する。
